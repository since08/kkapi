# meta info
json.partial! 'common/meta'
# code & msg
json.partial! 'common/api_result', api_result: ApiResult.success_result
# data
json.data do
  json.categories do
    json.partial! 'categories', categories: @category.children
  end
end